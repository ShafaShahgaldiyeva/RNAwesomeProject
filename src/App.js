import React from 'react';
import { View } from 'react-native';
import SudokuBoard from './SudokuBoard';

const App = () => {
  return (
    <View>
      <SudokuBoard />
    </View>
  );
};

export default App;
